srcdir=@srcdir@
VPATH=$(srcdir)
top_builddir=@top_builddir@

PROG=rectmul

include ../../Makefile.wf
include $(top_builddir)/util/Makefile.use_us

#$(PROG): LDLIBS+=$(HOME)/ray/lib/libgoto2_nehalem-r1.13_T1.a

$(PROG): $(PROG).cc $(top_builddir)/util/getoptions.o
$(PROG): $(SCHEDULER_GOALS)

# To include getoptions.h
CFLAGS += -I$(top_builddir)/util -I../common -I$(srcdir)/../common
CXXFLAGS += -I$(top_builddir)/util -I../common -I$(srcdir)/../common

clean:
	rm -f $(PROG)
